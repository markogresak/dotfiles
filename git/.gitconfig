[credential]
	helper = osxkeychain
[user]
	name = Marko Grešak
	email = marko@gresak.io
	editor = vim
	signingkey = 534C8F2248DA2309
[commit]
	gpgsign = true
[gpg]
	program = /usr/local/bin/gpg
[core]
	editor = vim
	excludesfile = /Users/markogresak/.gitignore_global
	autocrlf = input
	pager = diff-so-fancy | less --tabs=2 -RFX
[color]
	ui = true
[push]
	default = current
[pull]
	default = current
[filter "hawser"]
	clean = git hawser clean %f
	smudge = git hawser smudge %f
	required = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

