# export LLVM=/usr/local/opt/emscripten/libexec/llvm/bin
# export BINARYEN="$(brew --prefix binaryen)"
# `brew --prefix` is slow, override with hardcoded value to improve speed
# export BINARYEN="/usr/local/opt/binaryen"
